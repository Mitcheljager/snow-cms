<div class="form">
  <%= form_for(@website_settings) do |f| %>
    <% if @website_settings.errors.any? %>
      <div id="error_explanation">
        <h2>
          <%= pluralize(@website_settings.errors.count, 'error') %> prohibited
          this website_settings from being saved:
        </h2>
        <ul>
          <% @website_settings.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <%= f.text_field :title, placeholder: 'Title', class: 'form__field form__field--large' %>

    <%= f.submit class: 'btn btn--large' %>
  <% end %>
</div>
